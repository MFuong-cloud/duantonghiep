các câu lệnh cần khi chạy BE
chạy lần lượt các câu lệnh sau 
composer install
php artisan key:generate
php artisan migrate
php artisan storage:link
php artisan ser

Tạo terminal riêng để chạy

php artisan queue:work
